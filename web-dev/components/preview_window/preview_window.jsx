"use strict";

/**
 * This component implements a 3D view into the current scene being edited.
 */
var PreviewWindow = React.createClass({
    render: function() {
        return ( <div className="preview-window"></div> );
    }
});
